# Python
__pycache__/
*.py[cod]
*$py.class
*.egg-info/
.pytest_cache/
.ruff_cache/
.mypy_cache/
.pyright/
.coverage
htmlcov/

# Virtual envs
.venv/
venv/
env/

# Env / secrets
.env
.env.*
!.env.example

# IDE / OS
.DS_Store
.idea/
.vscode/
*.swp

# Claude Code — local settings and agent worktrees (never commit)
.claude/

# Build artifacts
build/
dist/
*.egg

# Local data
eval/results/local/
*.sqlite
*.db

# Prometheus bearer token for /metrics scraping — must NOT be committed.
# Use observability/auth_token.example as a template.
observability/auth_token

# Crucible review run artifacts (stage outputs, intermediate JSON) — reports are tracked
.review/runs/
